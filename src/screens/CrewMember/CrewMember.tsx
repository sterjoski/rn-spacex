import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const CrewMember = () => {
    return (
        <SafeAreaView>
            <Text>Crew Member (1)</Text>
        </SafeAreaView>
    )
}

export default CrewMember
